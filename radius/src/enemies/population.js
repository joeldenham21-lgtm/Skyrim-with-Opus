// STUB — owned by the enemies-core agent. Populates POIs per tide level, keeps spawns out of view, respawns on Tide.
export function createPopulation(ctx) {
  return { update(dt) {}, populate() {}, reset() {} };
}
