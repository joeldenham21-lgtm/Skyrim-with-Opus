// STUB — owned by the characters agent. Worn gear meshes for characters: buildVest(id), buildHelmet(id), buildPack(id) -> Group (or null).
export function buildVest(id) { return null; }
export function buildHelmet(id) { return null; }
export function buildPack(id) { return null; }
