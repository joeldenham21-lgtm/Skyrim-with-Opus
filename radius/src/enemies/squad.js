// STUB — owned by the enemies-core agent. Squad coordination for mimics: roles, cover assignment, flank paths, regroup.
export function createSquads(ctx) {
  return { list: [], form(members, poi) { return null; }, update(dt) {} };
}
