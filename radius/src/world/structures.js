// STUB — owned by the structures agent. Village, substation, church, checkpoint, convoy, rail, pylons, fences. Registers colliders, cover, spawn/loot spots.
export function createStructures(ctx) {
  return { update(dt, t) {}, reset() {} };
}
