// STUB — owned by the anomalies agent. Artifacts near anomalies; pickup via ctx.interact.
export function createArtifacts(ctx) {
  const list = [];
  return { list, spawn(type, position) { return null; }, populate() {}, reset() {}, update(dt) {} };
}
