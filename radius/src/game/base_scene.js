// STUB — owned by the structures agent. The Vanno bunker: interior, door, stations (terminal/workbench/supply/bed/storage).
export function createBase(ctx) {
  return { update(dt) {}, stations: {} };
}
