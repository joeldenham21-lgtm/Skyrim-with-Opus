// STUB — owned by the weapons agent. Procedural weapon meshes: buildGun(id) -> THREE.Group with named parts.
import * as THREE from 'three';
export function buildGun(id) { return new THREE.Group(); }
